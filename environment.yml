name: colab-env
channels:
  - nvidia           # 1️⃣
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pytorch=2.6.0
  - pytorch-cuda=12.4
  - nvidia::cuda-toolkit=12.4    # 2️⃣ satisface cuda-cudart y cuda-nvtx
  - scikit-learn=1.6.1
  - optuna=4.4.0
  - numpy<2.0                   # 3️⃣ evita choque con torchvision
  - pandas=2.2.2
  - seaborn=0.13.2
  # - torchvision   # Comenta o elimina si no lo necesitas
  - pip
